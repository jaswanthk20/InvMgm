services:
  - type: web
    name: invmgm
    env: python
    buildCommand: "pip install -r requirements.txt && flask --app app init-db"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
